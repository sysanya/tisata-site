# Статический сайт — корень репозитория = публичная папка
[build]
  publish = "."
  command = "echo 'static site, no build'"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
